# Only two local env files (both gitignored):
#   cp .env.example .env.development   # npm run dev  (NODE_ENV=development)
#   cp .env.example .env.production    # npm run prod (NODE_ENV=production)
# Do not use .env or .env.local.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_ENVIRONMENT=development
NEXT_PUBLIC_USE_TRIAGE_WS=0
