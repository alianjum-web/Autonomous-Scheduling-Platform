import { AcceptInviteScreen } from "@/components/auth/screens/AcceptInviteScreen";

export const dynamic = "force-dynamic";
export const metadata = { title: "Accept staff invite" };

export default function AcceptInvitePage() {
  return <AcceptInviteScreen />;
}
